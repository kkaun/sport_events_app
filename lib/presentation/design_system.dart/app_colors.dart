import 'dart:ui';

/// It's always good to start at least with a simple design system.
/// Not adding fonts' presets in this directory in order to save time.
class AppColors {
  static Color primary = const Color(0xFF171E31);
  static Color secondary = const Color(0xFFE7F4F8);
}
